<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gmpillatt.upsanddowns">

    <!--Insert in above for testing a shared DB: android:sharedUserId="com.gmp.gmp"-->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".AppSetup">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ConfirmUpsDowns"/>
        <activity android:name=".ListAll"/>
        <activity android:name=".EditLock"/>
        <activity android:name=".Chart1"/>
        <activity android:name=".Chart2"/>
        <activity android:name=".Chart3" />
        <activity android:name=".exportData"/>
    </application>

</manifest>